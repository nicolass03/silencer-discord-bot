services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile.gpu
    image: silencer-discord-bot:gpu
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
